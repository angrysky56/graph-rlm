---
phase: 02-core-abstractions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/core/circuit.py"
  - "src/core/exceptions/circuit.py"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "CircuitBreaker can be instantiated with configurable failure_threshold, timeout, success_threshold"
    - "Circuit state transitions (CLOSED→OPEN→HALF_OPEN→CLOSED) are observable via logs"
    - "CircuitOpenError is raised when circuit is OPEN with proper exception context"
  artifacts:
    - path: "src/core/circuit.py"
      provides: "CircuitState enum, CircuitBreakerConfig dataclass, CircuitBreaker class"
      min_lines: 150
    - path: "src/core/exceptions/circuit.py"
      provides: "CircuitOpenError exception extending BaseGraphRLMError"
      exports: ["CircuitOpenError"]
  key_links:
    - from: "src/core/circuit.py"
      to: "src/core/exceptions/circuit.py"
      via: "CircuitOpenError import"
    - from: "src/core/circuit.py"
      to: "structlog"
      via: "Structured logging for state transitions"
---

<objective>
Implement core circuit breaker infrastructure: CircuitState enum, CircuitBreakerConfig dataclass, CircuitBreaker class with async-aware state machine, and CircuitOpenError exception.

**Purpose:** Establish the foundational abstraction that protects Graph-RLM from cascading failures when external services (LLM, MCP) become unavailable.

**Output:** Runnable CircuitBreaker class ready for LLM and MCP integration in plans 02 and 03.
</objective>

<execution_context>
@/home/ty/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ty/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-abstractions/02-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md

**From RESEARCH.md:**
- Custom async-aware circuit breaker required (no library meets Python 3.13 needs)
- Three-state machine: CLOSED → OPEN → HALF_OPEN → CLOSED
- Async lock for thread-safe state transitions
- Integration with Phase 1 exception hierarchy and structlog

**Key patterns:**
```python
class CircuitState(Enum):
    CLOSED = auto()   # Normal operation, counting failures
    OPEN = auto()     # Rejecting calls immediately  
    HALF_OPEN = auto() # Testing recovery, limited calls
```

**User constraints:** None (no CONTEXT.md for this phase)
</context>

<tasks>

<task type="auto">
  <name>Create CircuitState enum and CircuitBreakerConfig dataclass</name>
  <files>src/core/circuit.py</files>
  <action>
Create src/core/circuit.py with:

1. **CircuitState enum** with CLOSED, OPEN, HALF_OPEN states
2. **CircuitBreakerConfig dataclass** with:
   - failure_threshold: int = 5
   - timeout_seconds: float = 60.0
   - success_threshold: int = 3
3. Import BaseGraphRLMError and ErrorCode from src.core.exceptions (Phase 1 modules)

Use enum.auto() for state values. Add proper type hints. Document each class with docstrings.
  </action>
  <verify>
grep -q "class CircuitState" src/core/circuit.py && grep -q "class CircuitBreakerConfig" src/core/circuit.py && grep -q "failure_threshold" src/core/circuit.py</verify>
  <done>
CircuitState enum and CircuitBreakerConfig dataclass exist in src/core/circuit.py with proper type hints and docstrings</done>
</task>

<task type="auto">
  <name>Implement CircuitOpenError exception</name>
  <files>src/core/exceptions/circuit.py</files>
  <action>
Create src/core/exceptions/circuit.py with:

1. **CircuitOpenError class** extending BaseGraphRLMError
2. Constructor accepting:
   - message: str
   - correlation_id: str | None = None
3. Uses ErrorCode.CIRCUIT_OPEN from Phase 1 exception hierarchy
4. Includes proper docstring explaining when this exception is raised

Example structure:
```python
class CircuitOpenError(BaseGraphRLMError):
    """Raised when circuit breaker is open and calls are rejected."""
    
    def __init__(self, message: str, correlation_id: str | None = None):
        super().__init__(
            message=message,
            error_code=ErrorCode.CIRCUIT_OPEN,
            correlation_id=correlation_id
        )
```
  </action>
  <verify>
grep -q "class CircuitOpenError" src/core/exceptions/circuit.py && grep -q "BaseGraphRLMError" src/core/exceptions/circuit.py</verify>
  <done>
CircuitOpenError exception extends BaseGraphRLMError with proper error code and correlation ID support</done>
</task>

<task type="auto">
  <name>Implement async-aware CircuitBreaker class with state machine</name>
  <files>src/core/circuit.py</files>
  <action>
Extend src/core/circuit.py with CircuitBreaker class:

1. **Constructor** accepting:
   - name: str
   - config: CircuitBreakerConfig
   - logger: Logger | None = None (structlog logger)

2. **State management**:
   - _state: CircuitState (private, starts CLOSED)
   - _failure_count: int = 0
   - _success_count: int = 0
   - _last_failure_time: datetime | None = None
   - _lock: asyncio.Lock for thread-safe transitions

3. **state property**: Returns current state, automatically transitions OPEN → HALF_OPEN after timeout

4. **call_async method**: 
   - Takes async function and args/kwargs
   - If OPEN: raises CircuitOpenError immediately
   - If CLOSED/HALF_OPEN: executes function, calls _on_success/_on_failure
   - All state transitions use async lock

5. **_on_success method**: 
   - HALF_OPEN: increments success_count, transitions to CLOSED when success_threshold reached
   - CLOSED: resets failure_count to 0

6. **_on_failure method**:
   - CLOSED: increments failure_count, transitions to OPEN when failure_threshold reached
   - HALF_OPEN: transitions back to OPEN immediately

7. **Structured logging**: Log all state transitions with circuit name and relevant context

8. **Type hints**: Full typing with async/await support for Python 3.13
  </action>
  <verify>
grep -q "class CircuitBreaker" src/core/circuit.py && grep -q "async def call_async" src/core/circuit.py && grep -q "async def _on_success" src/core/circuit.py</verify>
  <done>
CircuitBreaker class implements async-aware state machine with proper locking, state transitions, and structured logging</done>
</task>

</tasks>

<verification>
- CircuitBreaker can be instantiated with default and custom configurations
- State transitions are observable via structlog: CLOSED→OPEN on failure_threshold, OPEN→HALF_OPEN after timeout, HALF_OPEN→CLOSED on success_threshold
- CircuitOpenError raised when calling through open circuit with correlation_id preserved
- All methods are async and use proper asyncio patterns
</verification>

<success_criteria>
- CircuitBreaker can be instantiated with configurable failure_threshold, timeout, success_threshold
- Circuit state transitions (CLOSED→OPEN→HALF_OPEN→CLOSED) are observable via logs
- CircuitOpenError is raised when circuit is OPEN with proper exception context
- 100% type coverage with Python 3.13 async support
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-abstractions/02-01-SUMMARY.md`
</output>
