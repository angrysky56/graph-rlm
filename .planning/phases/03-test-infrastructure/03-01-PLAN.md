---
phase: 03-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, tests/conftest.py, tests/mocking/__init__.py, tests/mocking/mocks.py]
autonomous: true

must_haves:
  truths:
    - "Developer can run pytest and have asyncio_mode=auto enabled"
    - "Developer can access MockRegistry from tests/mocking/mocks.py"
    - "MockRegistry supports FalkorDB, LLM service, and external API mocks"
    - "MockRegistry.reset() clears state between test runs"
  artifacts:
    - path: "tests/conftest.py"
      provides: "pytest configuration with asyncio fixtures"
      min_lines: 30
    - path: "tests/mocking/mocks.py"
      provides: "MockRegistry class with register/get/reset"
      min_lines: 50
    - path: "tests/mocking/__init__.py"
      provides: "Package exports for mocking module"
      min_lines: 5
    - path: "pyproject.toml"
      provides: "pytest asyncio_mode=auto and pytest-cov configuration"
      contains: "asyncio_mode"
  key_links:
    - from: "pyproject.toml"
      to: "tests/conftest.py"
      via: "pytest configuration directive"
      pattern: "asyncio_mode.*auto"
    - from: "tests/conftest.py"
      to: "tests/mocking/mocks.py"
      via: "MockRegistry fixture import"
      pattern: "from tests.mocking.mocks import MockRegistry"
    - from: "tests/mocking/mocks.py"
      to: "pytest"
      via: "MagicMock, AsyncMock imports"
      pattern: "from unittest.mock import"
---

<objective>
Establish pytest configuration with asyncio_mode=auto and create MockRegistry class for centralized mock management.

Purpose: Foundation for all Phase 3 testing. Without pytest config, no tests can run. Without MockRegistry, no external dependencies can be mocked.
Output:
- pyproject.toml with pytest configuration
- tests/conftest.py with async fixtures
- tests/mocking/mocks.py with MockRegistry class
</objective>

<execution_context>
@/home/ty/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ty/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-test-infrastructure/03-RESEARCH.md
@.planning/STATE.md

# Research guidance for test patterns
@03-RESEARCH.md#pattern-1-pytest-asyncio-configuration
@03-RESEARCH.md#pattern-2-mockregistry-class
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure pytest and pytest-cov in pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
    Add or update [tool.pytest.ini_options] section in pyproject.toml:

    [tool.pytest.ini_options]
    asyncio_mode = "auto"
    asyncio_default_fixture_loop_scope = "function"
    testpaths = ["tests"]
    python_files = ["test_*.py"]
    python_classes = ["Test*"]
    python_functions = ["test_*"]
    addopts = "-v --tb=short"

    Also add [tool.coverage.run] and [tool.coverage.report] sections:

    [tool.coverage.run]
    source = ["src.core"]
    omit = ["*/tests/*", "*/__pycache__/*"]

    [tool.coverage.report]
    exclude_lines = [
        "pragma: no cover",
        "def __repr__",
        "raise AssertionError",
        "raise NotImplementedError",
    ]

    Reference existing pyproject.toml structure to maintain consistency.
  </action>
  <verify>grep -q "asyncio_mode.*auto" pyproject.toml && grep -q "tool.coverage.run" pyproject.toml</verify>
  <done>pyproject.toml contains pytest asyncio_mode=auto and pytest-cov configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/conftest.py with async fixtures</name>
  <files>tests/conftest.py</files>
  <action>
    Create tests/conftest.py with:

    1. Import pytest, asyncio, and MockRegistry from tests.mocking.mocks
    2. Session-scoped event_loop fixture for proper async lifecycle
    3. Function-scoped mock_registry fixture that yields a fresh MockRegistry and resets on teardown
    4. Any other base fixtures needed for async testing (参照03-RESEARCH.md#pattern-1)

    Use asyncio_mode="auto" compatible patterns. Ensure proper scope management to avoid "event loop closed" errors.
  </action>
  <verify>python -c "import tests.conftest; print('conftest.py loads successfully')"</verify>
  <done>tests/conftest.py exists with event_loop fixture and mock_registry fixture</done>
</task>

<task type="auto">
  <name>Task 3: Create MockRegistry class in tests/mocking/mocks.py</name>
  <files>tests/mocking/mocks.py</files>
  <action>
    Create tests/mocking/mocks.py with MockRegistry class:

    Class MockRegistry:
    - __init__: Initialize empty mocks dict, reset_history list
    - register(name, mock): Store mock by name
    - get(name): Retrieve mock by name
    - reset(): Clear all mocks and reset_history
    - falkordb property: Return FalkorDB mock
    - llm property: Return LLM service mock
    - external property: Return external API mock

    Import MagicMock, AsyncMock from unittest.mock. Use type hints for all methods and properties.
  </action>
  <verify>python -c "from tests.mocking.mocks import MockRegistry; r = MockRegistry(); r.reset(); print('MockRegistry works')"</verify>
  <done>tests/mocking/mocks.py exists with MockRegistry class supporting register/get/reset and property accessors</done>
</task>

<task type="auto">
  <name>Task 4: Create tests/mocking/__init__.py</name>
  <files>tests/mocking/__init__.py</files>
  <action>
    Create tests/mocking/__init__.py that exports:
    - MockRegistry
    - Any mock fixtures or utilities needed by tests

    Keep minimal for now - can add more exports as needed during implementation.
  </action>
  <verify>python -c "from tests.mocking import MockRegistry; print('MockRegistry importable from tests.mocking')"</verify>
  <done>tests/mocking/__init__.py exists with appropriate exports</done>
</task>

</tasks>

<verification>
Run `pytest --version` to confirm pytest loads with asyncio_mode.
Run `python -c "from tests.mocking.mocks import MockRegistry; r = MockRegistry(); r.reset(); print('MockRegistry OK')"`.
Verify coverage configuration by running `pytest --cov --help` output includes configured options.
</verification>

<success_criteria>
1. pytest executes with asyncio_mode=auto (no import errors)
2. MockRegistry class exists and supports register/get/reset
3. FalkorDB, LLM, external properties return registered mocks
4. mock_registry fixture in conftest.py provides fresh registry per test
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-infrastructure/03-01-SUMMARY.md`
</output>