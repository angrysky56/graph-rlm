---
phase: 03-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [tests/conftest.py, tests/mocking/falkordb.py, tests/mocking/llm.py, tests/mocking/external.py]
autonomous: true

must_haves:
  truths:
    - "Developer can use async fixtures that properly clean up after each test"
    - " FalkorDB mock provides session-based query interface"
    - "LLM service mock supports ainvoke and abatch async methods"
    - "External API mock supports HTTP request/response patterns"
    - "MockRegistry fixture is available in all tests"
  artifacts:
    - path: "tests/mocking/falkordb.py"
      provides: "FalkorDB mock fixtures and utilities"
      min_lines: 30
    - path: "tests/mocking/llm.py"
      provides: "LLM service mock fixtures (LangChain integration)"
      min_lines: 40
    - path: "tests/mocking/external.py"
      provides: "HTTP/API mock utilities"
      min_lines: 30
    - path: "tests/conftest.py"
      provides: "Updated base fixtures for async setup/teardown"
      contains: "mock_registry_with_falkordb"
      contains: "mock_registry_with_llm"
  key_links:
    - from: "tests/mocking/falkordb.py"
      to: "tests/mocking/mocks.py"
      via: "MockRegistry.register calls"
      pattern: "mock_registry\\.register\\('falkordb'"
    - from: "tests/mocking/llm.py"
      to: "tests/mocking/mocks.py"
      via: "MockRegistry.register calls"
      pattern: "mock_registry\\.register\\('llm'"
    - from: "tests/conftest.py"
      to: "tests/mocking/falkordb.py"
      via: "fixture imports"
      pattern: "from tests.mocking.falkordb import"
    - from: "tests/conftest.py"
      to: "tests/mocking/llm.py"
      via: "fixture imports"
      pattern: "from tests.mocking.llm import"
---

<objective>
Create FalkorDB, LLM service, and external API mocks; enhance conftest.py with comprehensive base fixtures.

Purpose: After MockRegistry foundation, this wave creates specific mocks for external dependencies and fixtures for async setup/teardown. Mocks enable testing without real services.
Output:
- tests/mocking/falkordb.py (FalkorDB mock)
- tests/mocking/llm.py (LLM service mock)
- tests/mocking/external.py (HTTP/API mock)
- Updated tests/conftest.py with fixtures for each mock type
</objective>

<execution_context>
@/home/ty/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ty/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-test-infrastructure/03-RESEARCH.md

# Reference mock patterns from research
@03-RESEARCH.md#pattern-3-falkordb-mock
@03-RESEARCH.md#pattern-4-llm-service-mock
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FalkorDB mock in tests/mocking/falkordb.py</name>
  <files>tests/mocking/falkordb.py</files>
  <action>
    Create tests/mocking/falkordb.py with FalkorDB mock utilities:

    1. mock_falkordb() fixture:
       - Create MagicMock client with session attribute
       - session.query returns MagicMock (sync) or AsyncMock (async)
       - client.close is MagicMock

    2. mock_registry_with_falkordb(registry, mock_falkordb) fixture:
       - Register 'falkordb' mock in registry
       - Yield registry

    3. Any additional helpers for FalkorDB query patterns

    Use patterns from 03-RESEARCH.md#pattern-3. Ensure async support via AsyncMock.
  </action>
  <verify>python -c "from tests.mocking.falkordb import mock_falkordb; print('falkordb.py loads')"</verify>
  <done>tests/mocking/falkordb.py exists with mock_falkordb fixture and mock_registry_with_falkordb fixture</done>
</task>

<task type="auto">
  <name>Task 2: Create LLM service mock in tests/mocking/llm.py</name>
  <files>tests/mocking/llm.py</files>
  <action>
    Create tests/mocking/llm.py with LLM service mock utilities:

    1. mock_llm_service() fixture:
       - Create MagicMock with ChatOpenAI interface
       - ainvoke returns AsyncMock with mocked response
       - abatch returns AsyncMock with list of responses
       - Support streaming if applicable

    2. mock_registry_with_llm(registry, mock_llm_service) fixture:
       - Register 'llm' mock in registry
       - Optionally patch LangChain imports

    3. Any additional helpers for LLM call patterns

    Use patterns from 03-RESEARCH.md#pattern-4. Ensure langchain_openai patching works if needed.
  </action>
  <verify>python -c "from tests.mocking.llm import mock_llm_service; print('llm.py loads')"</verify>
  <done>tests/mocking/llm.py exists with mock_llm_service fixture and mock_registry_with_llm fixture</done>
</task>

<task type="auto">
  <name>Task 3: Create external API mock in tests/mocking/external.py</name>
  <files>tests/mocking/external.py</files>
  <action>
    Create tests/mocking/external.py with HTTP/API mock utilities:

    1. mock_http_client() fixture:
       - Create MagicMock for HTTP client (aiohttp/httpx style)
       - get/post/put/delete return AsyncMock with mocked response
       - response.json() returns MagicMock

    2. mock_registry_with_external(registry, mock_http_client) fixture:
       - Register 'external' mock in registry

    3. Any additional helpers for HTTP patterns

    Keep generic enough to support various HTTP clients (aiohttp, httpx, requests).
  </action>
  <verify>python -c "from tests.mocking.external import mock_http_client; print('external.py loads')"</verify>
  <done>tests/mocking/external.py exists with mock_http_client fixture and mock_registry_with_external fixture</done>
</task>

<task type="auto">
  <name>Task 4: Enhance conftest.py with base fixtures</name>
  <files>tests/conftest.py</files>
  <action>
    Update tests/conftest.py to import and provide fixtures from mocking modules:

    1. Import mock_falkordb from tests.mocking.falkordb
    2. Import mock_llm_service from tests.mocking.llm
    3. Import mock_http_client from tests.mocking.external
    4. Add composite fixtures if helpful (e.g., mock_all_services)

    Do NOT override existing mock_registry fixture - keep it simple for now.

    Reference 03-RESEARCH.md#pattern-1 for proper async fixture patterns.
  </action>
  <verify>grep -q "from tests.mocking" tests/conftest.py</verify>
  <done>tests/conftest.py imports mock fixtures from mocking submodules</done>
</task>

</tasks>

<verification>
Run `pytest tests/ --collect-only` to verify all fixtures are discoverable.
Test that mock_registry fixture can access falkordb, llm, external properties.
Verify async methods on mocks work (e.g., await mock.ainvoke()).
</verification>

<success_criteria>
1. FalkorDB mock provides session.query interface (sync and async)
2. LLM service mock supports ainvoke/abatch async methods
3. External API mock supports HTTP request patterns
4. mock_registry fixture works with all mock types
5. Fixtures properly clean up (no state leakage between tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-infrastructure/03-02-SUMMARY.md`
</output>