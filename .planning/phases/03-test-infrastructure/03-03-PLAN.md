---
phase: 03-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: [tests/unit/__init__.py, tests/unit/test_config.py, tests/unit/test_exceptions/__init__.py, tests/unit/test_exceptions/test_base.py, tests/legacy/__init__.py, tests/legacy/test_agent.py]
autonomous: true

must_haves:
  truths:
    - "Developer can achieve 100% coverage on src/core/config.py"
    - "Developer can achieve 100% coverage on src/core/exceptions/base.py"
    - "Developer can write unit tests for agent.py using legacy mocking patterns"
    - "All tests pass when running pytest"
  artifacts:
    - path: "tests/unit/__init__.py"
      provides: "Package initialization for unit tests"
      min_lines: 5
    - path: "tests/unit/test_config.py"
      provides: "100% coverage tests for config.py"
      min_lines: 100
    - path: "tests/unit/test_exceptions/__init__.py"
      provides: "Package initialization for exception tests"
      min_lines: 5
    - path: "tests/unit/test_exceptions/test_base.py"
      provides: "100% coverage tests for exceptions/base.py"
      min_lines: 100
    - path: "tests/legacy/__init__.py"
      provides: "Package initialization for legacy tests"
      min_lines: 5
    - path: "tests/legacy/test_agent.py"
      provides: "Isolated test module for agent.py with legacy mocking"
      min_lines: 50
  key_links:
    - from: "tests/unit/test_config.py"
      to: "src/core/config.py"
      via: "import statements"
      pattern: "from src.core.config import"
    - from: "tests/unit/test_exceptions/test_base.py"
      to: "src/core/exceptions/base.py"
      via: "import statements"
      pattern: "from src.core.exceptions.base import"
    - from: "tests/legacy/test_agent.py"
      to: "src/core/agent.py"
      via: "import statements"
      pattern: "from src.core.agent import"
    - from: "tests/unit/test_exceptions/test_base.py"
      to: "tests/mocking/mocks.py"
      via: "MockRegistry usage"
      pattern: "from tests.mocking.mocks import MockRegistry"
---

<objective>
Write unit tests achieving 100% coverage on config.py and exceptions/base.py, plus legacy mocking tests for agent.py.

Purpose: Core test coverage goals. config.py and exceptions/base.py are Phase 1 modules requiring comprehensive testing. agent.py needs legacy mocking patterns (REFR-02).
Output:
- tests/unit/test_config.py (100% coverage)
- tests/unit/test_exceptions/test_base.py (100% coverage)
- tests/legacy/test_agent.py (legacy mocking patterns)
</objective>

<execution_context>
@/home/ty/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ty/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference source files to understand testing targets
src/core/config.py: Config module from Phase 1
src/core/exceptions/base.py: Base exception classes from Phase 1
src/core/agent.py: Agent module requiring legacy mocking (REFR-02)

# Coverage approach from research
@03-RESEARCH.md#pitfall-4-coverage-gaps-from-exception-handlers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/unit/__init__.py and tests/unit/test_exceptions/__init__.py</name>
  <files>tests/unit/__init__.py, tests/unit/test_exceptions/__init__.py</files>
  <action>
    Create package initialization files:

    1. tests/unit/__init__.py - can be empty or provide common imports
    2. tests/unit/test_exceptions/__init__.py - can be empty

    These are required for Python to treat directories as packages.
  </action>
  <verify>python -c "import tests.unit; import tests.unit.test_exceptions; print('Packages importable')"</verify>
  <done>tests/unit/__init__.py and tests/unit/test_exceptions/__init__.py exist</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for src/core/config.py (100% coverage)</name>
  <files>tests/unit/test_config.py</files>
  <action>
    Create tests/unit/test_config.py:

    1. Read src/core/config.py to identify all classes, functions, and edge cases
    2. Write tests for:
       - Settings class initialization
       - All config properties and their validation
       - Environment variable loading
       - Type conversions and validation
       - Edge cases (missing env vars, invalid values)

    3. Target 100% coverage - run pytest-cov to identify gaps
    4. Use mock_registry fixture for any external service needs

    Follow 03-RESEARCH.md#pitfall-4 guidance: test all paths, including exception handlers.
  </action>
  <verify>pytest tests/unit/test_config.py --cov=src.core.config --cov-report=term-missing</verify>
  <done>tests/unit/test_config.py achieves 100% coverage on src/core/config.py</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for src/core/exceptions/base.py (100% coverage)</name>
  <files>tests/unit/test_exceptions/test_base.py</files>
  <action>
    Create tests/unit/test_exceptions/test_base.py:

    1. Read src/core/exceptions/base.py to identify all exception classes and methods
    2. Write tests for:
       - BaseGraphRLMError initialization with error_code, correlation_id, cause
       - ErrorCode enum values and hierarchy
       - Specific exception subclasses (GraphError, SkillExecutionError, etc.)
       - to_dict() serialization for all exception types
       - __str__ and __repr__ methods
       - Exception chaining (cause parameter)

    3. Target 100% coverage - run pytest-cov to identify gaps
    4. Test both happy path and error paths (e.g., invalid error codes)

    Reference 03-RESEARCH.md#pitfall-4 for exception testing patterns.
  </action>
  <verify>pytest tests/unit/test_exceptions/test_base.py --cov=src.core.exceptions.base --cov-report=term-missing</verify>
  <done>tests/unit/test_exceptions/test_base.py achieves 100% coverage on src/core/exceptions/base.py</done>
</task>

<task type="auto">
  <name>Task 4: Create tests/legacy/__init__.py and write agent.py tests</name>
  <files>tests/legacy/__init__.py, tests/legacy/test_agent.py</files>
  <action>
    Create tests/legacy/__init__.py for package initialization.

    Create tests/legacy/test_agent.py with legacy mocking patterns for src/core/agent.py:

    1. Read src/core/agent.py to understand its dependencies and interfaces
    2. Use legacy mocking patterns (likely unittest.mock directly, not pytest-mock fixtures)
    3. Mock external dependencies (FalkorDB, LLM service) using mocks from tests/mocking/
    4. Test core agent functionality in isolation

    "Legacy mocking" means match existing patterns in codebase - check if there are existing tests for guidance.
  </action>
  <verify>pytest tests/legacy/test_agent.py --collect-only</verify>
  <done>tests/legacy/test_agent.py exists with legacy mocking patterns for agent.py</done>
</task>

</tasks>

<verification>
Run `pytest tests/unit/test_config.py --cov=src.core.config --cov-report=term-missing` - confirm 100% coverage
Run `pytest tests/unit/test_exceptions/test_base.py --cov=src.core.exceptions.base --cov-report=term-missing` - confirm 100% coverage
Run `pytest tests/legacy/test_agent.py` - confirm tests execute without errors
Run `pytest tests/` - confirm all Phase 3 tests pass
</verification>

<success_criteria>
1. tests/unit/test_config.py achieves 100% coverage on src/core/config.py
2. tests/unit/test_exceptions/test_base.py achieves 100% coverage on src/core/exceptions/base.py
3. tests/legacy/test_agent.py executes with legacy mocking patterns
4. All Phase 3 tests pass (no failures or errors)
5. pytest-cov reports coverage for configured modules
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-infrastructure/03-03-SUMMARY.md`
</output>